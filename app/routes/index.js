import Route from '@ember/routing/route';

export default class IndexRoute extends Route {
    //TODO
    //create a bromileys model
    //create a bromileys adapter
    //proxy to express server
    //display brom JSON's in the index route
}
